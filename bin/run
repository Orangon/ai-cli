#!/usr/bin/env node

require('dotenv/config');
require('dotenv').config({ path: require('path').join(require('os').homedir(), '.ai-cli', '.env') });

const oclif = require('@oclif/core');

oclif.run().then(require('@oclif/core/flush')).catch(require('@oclif/core/handle'));
